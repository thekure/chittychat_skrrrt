syntax = "proto3";

package proto;

option go_package = "github.com/thekure/chittychat_skrrrt/proto";

message AskForClientName{
    string clientname = 1; // Not mandatory..
}

message TimeMessage{
    string time = 1; // This just increments, it's for ordering.
    string serverName = 2;
    // string example = 2;
    // string anotherExample = 3;
}

service TimeAskService{
    rpc GetTime (AskForClientName) returns (TimeMessage);
}